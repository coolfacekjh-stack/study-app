# 🤖 AI 활용 개발 과정 — 프롬프트 정리

> 이 문서는 학습 기록 관리 앱을 AI(Claude)와 함께 개발하면서 사용한 프롬프트를 시간 순서대로 정리한 기록입니다.

---

## 1단계 — 요구사항 분석 & 설계

---

### 프롬프트 001
```
React(Vite) 기반의 학습 기록 관리 앱을 만들려고 합니다.
프로젝트 구조를 설계해 주세요.

조건
- 컴포넌트 분리
- CRUD 구현 예정
- localStorage 사용 예정
- 이후 Supabase로 확장 가능하도록 설계
- 초보자가 이해하기 쉬운 구조
```
> **AI 응답 요약** : `components/`, `hooks/`, `services/`, `utils/` 폴더 구조 설계, services 레이어로 Supabase 확장 전략 제안

---

### 프롬프트 002
```
먼저 화면만 만들어줘
```
> **AI 응답 요약** : 더미 데이터 기반 UI 우선 구현 계획 수립, 로직 없이 컴포넌트 구성에 집중

---

## 2단계 — 컴포넌트 생성

---

### 프롬프트 003
```
프로젝트 구조에 맞춰 필요한 컴포넌트를 생성해 주세요.

StudyForm
StudyList
StudyItem
Header

각 컴포넌트는 기본 JSX와 export default까지 작성해 주세요.
```
> **AI 응답 요약** : 4개 컴포넌트 기본 뼈대 생성, 각 역할 분리

---

### 프롬프트 004
```
App.jsx를 작성해 주세요.

조건
- useState 사용
- studies 상태 생성
- Header / StudyForm / StudyList 연결
- 아직 CRUD 기능은 구현하지 않습니다.
```
> **AI 응답 요약** : 더미 데이터 배열 + useState로 studies 상태 생성, 컴포넌트 조합

---

### 프롬프트 005
```
Header 컴포넌트를 만들어 주세요.

제목은 📚 학습 기록 관리
아래에 간단한 설명도 추가해 주세요.
Bootstrap 없이 CSS만 사용합니다.
```
> **AI 응답 요약** : 보라색(#4f46e5) 헤더, 제목 + 부제목 구성, CSS-in-JS 스타일 객체 방식

---

### 프롬프트 006
```
StudyForm 컴포넌트를 작성해 주세요.

입력 항목
- 학습주제
- 공부시간
- 이해도
- 메모

저장 버튼 포함
아직 저장 기능은 구현하지 않습니다.
```
> **AI 응답 요약** : controlled component 패턴, `handleChange` + `form` 상태, `console.log`만 연결

---

## 3단계 — CSS 작성

---

### 프롬프트 007
```
현재 컴포넌트에 맞는 app.css를 작성해 주세요.

조건
- 카드형 디자인
- 깔끔한 흰색 배경
- 반응형
- 버튼 hover 효과
- 모바일에서도 보기 좋게
```
> **AI 응답 요약** : `.card` 공통 클래스, `box-shadow`, `@media (max-width: 480px)`, `.btn-primary:hover` 전환 효과

---

## 4단계 — CRUD 구현

---

### 프롬프트 008
```
StudyForm에 입력한 데이터를 App.jsx의 studies 상태에 추가하는 기능을 구현해 주세요.

조건
- useState 사용
- id 자동 생성
- createdAt 자동 저장
```
> **AI 응답 요약** : `addStudy` 함수, `Date.now()` id, `new Date().toISOString()` createdAt, `onAdd` props 콜백

---

### 프롬프트 009
```
StudyList 컴포넌트에서 studies 배열을 map()으로 출력하도록 구현해 주세요.
StudyItem 컴포넌트를 사용합니다.
```
> **AI 응답 요약** : `map()` + `key={study.id}`, `StudyItem` 분리 렌더링

---

### 프롬프트 010
```
삭제 버튼을 누르면 해당 학습기록이 삭제되도록 구현해 주세요.

조건
- filter() 사용
- App.jsx에서 상태 관리
```
> **AI 응답 요약** : `deleteStudy(id)` → `filter((s) => s.id !== id)`, props 드릴링 `App → StudyList → StudyItem`

---

### 프롬프트 011
```
수정 버튼을 누르면 기존 데이터를 입력창으로 가져와 수정 후 저장할 수 있도록 구현해 주세요.
```
> **AI 응답 요약** : `editTarget` 상태, `useEffect`로 폼 채우기, `map()`으로 해당 id 항목 교체, 추가/수정 모드 전환

---

### 프롬프트 012
```
입력값 검증을 추가해 주세요.

조건
- 학습주제 필수
- 공부시간은 1 이상
- 이해도는 1~5
```
> **AI 응답 요약** : `validate()` 함수, `errors` 상태, 에러 메시지 인라인 표시, 검증 실패 시 저장 중단

---

## 5단계 — localStorage 연동

---

### 프롬프트 013
```
현재 CRUD를 localStorage에 저장하도록 수정해 주세요.

조건
- 앱 실행 시 자동 불러오기
- 추가/수정/삭제 시 자동 저장
```
> **AI 응답 요약** : `services/localStorageService.js` 생성, `useState(() => getAll())` lazy initializer, `useEffect`로 자동 저장

---

### 프롬프트 014
```
등록된 학습기록이 없을 경우
"등록된 학습기록이 없습니다."
메시지를 출력하도록 구현해 주세요.
```
> **AI 응답 요약** : `studies.length === 0` 분기, `.empty-state` 스타일 추가

---

## 6단계 — 코드 품질 개선 (리팩토링)

---

### 프롬프트 015
```
현재 프로젝트를 분석해서 React 코드 품질을 개선해 주세요.

다음 항목을 검토해 주세요.
- 컴포넌트 분리 적절성
- 불필요한 코드 제거
- props 전달 방식
- state 관리 개선
- 가독성 향상
- React 권장 방식 적용

변경 이유도 설명해 주세요.
```
> **AI 응답 요약** : 5가지 개선 사항 도출 및 적용 (커스텀 훅, Fragment, 폼 필드 분리, 구조분해 일관성, try/catch)

---

### 프롬프트 016
```
현재 프로젝트를 분석해서 README.md를 작성해 주세요.

포함 내용
- 프로젝트 소개 / 주요 기능 / 사용 기술 / 설치 방법
- 실행 방법 / 프로젝트 구조 / AI 활용 내용
- 오류 해결 과정 / 향후 개선 사항
```
> **AI 응답 요약** : 완성된 README.md 생성 (표 형식, 코드 블록, 체크리스트 포함)

---

### 프롬프트 017
```
이번 프로젝트에서 AI를 활용한 개발 과정을 정리해 주세요.

다음 형식으로 작성해 주세요.
- 요구사항 분석 / 프로젝트 구조 설계 / 컴포넌트 생성
- CRUD 구현 / CSS 작성 / 오류 수정 / 최종 리팩토링
```
> **AI 응답 요약** : 단계별 개발 흐름 및 AI 기여 내용 정리 문서 생성

---

### 프롬프트 018
```
이번 프로젝트에서 사용했을 법한 AI 프롬프트를 시간순으로 정리해 주세요.
약 20개 정도 작성해 주세요.
```
> **AI 응답 요약** : 이 문서(AI_PROMPTS.md) 생성

---

### 프롬프트 019
```
AI 생성 결과 검토표를 표 형식으로 작성해 주세요.

항목
- 파일 구조 / 컴포넌트 / CRUD / CSS
- 입력 검증 / localStorage / React 코드 품질

검토 내용과 수정 여부도 포함해 주세요.
```
> **AI 응답 요약** : AI_REVIEW.md 생성 (7개 항목 검토표 + 수정 내역)

---

### 프롬프트 020
```
Supabase 연동을 위한 supabaseService.js 파일을 작성해 주세요.
localStorageService.js와 동일한 인터페이스(getAll, saveAll)로 작성해 주세요.
```
> **AI 응답 요약** : *(향후 진행 예정)* `supabaseService.js` 동일 인터페이스 템플릿

---

## 💡 AI 활용 핵심 전략

| 전략 | 설명 |
|------|------|
| **단계별 요청** | 한 번에 전체가 아니라 기능 단위로 순차 요청 |
| **조건 명시** | "아직 기능은 구현하지 않습니다" 등 범위 명확히 지정 |
| **피드백 루프** | AI 제안을 검토 후 수정 요청 반복 |
| **검토 요청** | 완성 후 "코드 품질을 개선해 주세요" 로 리팩토링 |
| **문서화 요청** | README, 프롬프트 정리 등 산출물도 AI와 함께 작성 |
